# Backend

API AI Finance Assistant: Express, TypeScript, Prisma, PostgreSQL.

## Запуск

```bash
cp .env.example .env
npm install
npx prisma generate
npm run dev
```

Сервер слушает порт из `PORT` (по умолчанию 3000).
